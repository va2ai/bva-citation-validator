node_modules/
.env
logs/*.jsonl
optimize/

# Generated by the recursive prompt optimizer (lib/prompt-loop.js).
# When absent, lib/context.js falls back to DEFAULT_GROUNDED_PROMPT.
system-prompt.txt

# Local agent / IDE session state — not part of the project.
.omc/
.claude/
.gemini/

# Personal portfolio dashboard data — not part of this project.
projects/

# AI assistant config — keep local, do not publish
CLAUDE.md
GEMINI.md
